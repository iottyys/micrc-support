@namespace("common")
protocol util {
    record Page {
        int current;
        int pageSize;
        int total;
        @java-class("java.util.ArrayList") array<Test> records;
        Test t;
    }

    record Test {
        string id;
    }
}
