@namespace("common")
protocol util {
    record Page {
        int current;
        int pageSize;
        int total;
        @java-class("java.util.ArrayList") array<@java-class("java.lang.Object") string> records;
        Test t;
    }

    record Test {
        string id;
    }
}
